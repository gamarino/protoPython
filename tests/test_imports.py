print("import difflib", flush=True)
import difflib
print("import inspect", flush=True)
import inspect
print("import pdb", flush=True)
import pdb
print("import unittest", flush=True)
import unittest
print("from collections import namedtuple", flush=True)
from collections import namedtuple
print("import _colorize", flush=True)
import _colorize
print("DONE", flush=True)
