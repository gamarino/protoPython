# protoPython: Next 20 Steps (v15)

Plan for 20 incremental milestones. Each step: implement → update docs → `git add` → commit.

**Prerequisite**: Next 20 Steps (v14) completed (steps 205–224).

**Status**: Completed.

---

## Steps 225–244

| Step | Description | Commit message |
|------|-------------|----------------|
| 225 | Builtin `eval(expr)` (stub) | done |
| 226 | Builtin `exec(code)` (stub) | done |
| 227 | `str.casefold()` | done |
| 228 | `str.isalpha()`, `str.isdigit()`, `str.isspace()` | done |
| 229 | `str.isalnum()` | done |
| 230 | `bytes.find()`, `bytes.count()` | done |
| 231 | `set.pop()` | done |
| 232 | `frozenset` basics | skipped (existing) |
| 233 | `itertools.accumulate` (stub) | done |
| 234 | `itertools.groupby` (stub) | done |
| 235 | collections defaultdict | skipped (verify) |
| 236 | `math.floor`, `math.ceil` | done (enhanced float) |
| 237 | `os.environ` stub | done |
| 238 | Exec test list.index | skipped |
| 239 | Foundation test str.isdigit | done |
| 240–244 | Docs, todo, v15 completion | done |

---

## Execution workflow per step

1. Implement code (include tests where applicable).
2. Update `docs/IMPLEMENTATION_PLAN.md` and `tasks/todo.md`.
3. `git add` all touched files.
4. Commit with the suggested message.
